<div class="status-bar">
    {{localize title}}

    <div class="bar-container {{#if (lt current min)}}below-min{{/if}}">
        <div class="progress-bar">
            <div class="full-bar"
            style="width: {{#percent current max}}{{/percent}}%">
            </div>
        </div>
    </div>
</div>