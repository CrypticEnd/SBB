<div class="tenet-focus-card" data-item-id="{{_id}}">
    <div class="card-name
     {{#if (eq type "Tenet")}}
        {{#if system.used}}
            crossed
        {{/if}}
    {{/if}}">
        {{name}}
    </div>
</div>